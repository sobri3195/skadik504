import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { setStorage,storageKeys } from '../utils/localStorage';
export default function Login(){const nav=useNavigate();const [form,setForm]=useState({username:'',password:'',role:'Peserta'});const [msg,setMsg]=useState('');const onSubmit=e=>{e.preventDefault(); if(!form.username||!form.password){setMsg('Data gagal: semua field wajib diisi.');return;} setStorage(storageKeys.currentUser,{username:form.username,role:form.role,loginAt:new Date().toISOString()}); setMsg('Data berhasil disimpan.'); nav('/dashboard')}; return <form className='card form' onSubmit={onSubmit}><h2>Login Dummy</h2>{msg&&<p>{msg}</p>}<input placeholder='Username' value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><input type='password' placeholder='Password' value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Peserta</option><option>Instruktur</option><option>Admin</option></select><button className='btn'>Login</button></form>}
